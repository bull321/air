import React from 'react';

const styles = {
  fontFamily: 'sans-serif',
  textAlign: 'center',
};

const App = () => (
  <div style={styles}>
    <h2>Start editing to see some magic happen {'\u2728'}</h2>
  </div>
);

export default App